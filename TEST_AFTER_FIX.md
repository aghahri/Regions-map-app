# تست بعد از Fix

## ✅ Flask Service در حال اجرا است!

حالا باید تست کنیم که همه چیز کار می‌کند:

## مراحل تست:

### 1. تست Flask (localhost:8000):

```bash
curl -I http://127.0.0.1:8000
```

**باید 200 OK بدهد**

### 2. تست nginx:

```bash
curl -I http://171.22.27.42
```

**باید 200 OK بدهد (نه 502)**

### 3. تست route لوگو:

```bash
curl -I "http://127.0.0.1:8000/uploads/logos/0e646b0e4600ce2bb5dd78845fe5e4f0_20251214_170341_IMG_1253.png"
```

**باید 200 OK بدهد**

### 4. تست از طریق nginx:

```bash
curl -I "http://171.22.27.42/uploads/logos/0e646b0e4600ce2bb5dd78845fe5e4f0_20251214_170341_IMG_1253.png"
```

**باید 200 OK بدهد (نه 404)**

### 5. بررسی وضعیت سرویس‌ها:

```bash
sudo systemctl status regions-map-app --no-pager | head -10
sudo systemctl status nginx --no-pager | head -10
```

---

## اگر همه تست‌ها OK بودند:

✅ سرور آماده است!
✅ می‌توانید از مرورگر تست کنید

---

## اگر هنوز مشکل دارید:

### بررسی لاگ‌ها:

```bash
# لاگ Flask
sudo journalctl -u regions-map-app -n 50

# لاگ nginx
sudo tail -f /var/log/nginx/error.log
```

---

**موفق باشید! 🚀**

