# راهنمای دیباگ مشکل نمایش لوگو در سایدبار

## 🔍 مراحل تست و دیباگ

### مرحله 1: آپدیت کد روی سرور

```bash
# اتصال به سرور
ssh user@your-server-ip

# رفتن به دایرکتوری
cd /var/www/regions-map-app/regions-map-app

# Pull آخرین تغییرات
git pull origin main

# Restart سرویس‌ها
sudo systemctl restart regions-map-app
sudo systemctl restart nginx
```

---

### مرحله 2: آپلود لوگو جدید

1. به پنل ادمین بروید
2. روی "مدیریت لینک‌های محلات" کلیک کنید
3. یک محله را انتخاب کنید
4. لوگو را آپلود کنید
5. **مهم:** نام محله را یادداشت کنید (مثلاً "تهران 3")

---

### مرحله 3: تست در صفحه اصلی

1. صفحه اصلی را باز کنید (refresh کنید)
2. نقشه را انتخاب کنید
3. روی همان محله‌ای که لوگو آپلود کردید کلیک کنید
4. **Developer Tools را باز کنید (F12)**
5. به تب **Console** بروید

---

### مرحله 4: بررسی لاگ‌های Console

در Console باید این لاگ‌ها را ببینید:

```javascript
// باید این لاگ‌ها را ببینید:
Sidebar properties: { ... }  // properties محله
Extracted values: { neighborhoodName: "...", ... }  // نام استخراج شده
Loading logo: { selectedMapId: "...", neighborhoodName: "..." }  // اطلاعات درخواست
Logo API response: { ... }  // پاسخ API
```

**اگر `selectedMapId` خالی است:**
- مشکل: `map_id` در URL نیست
- راه‌حل: مطمئن شوید که از `/map/{map_id}` یا `/?map_id={map_id}` استفاده می‌کنید

**اگر `neighborhoodName` خالی است:**
- مشکل: نام محله از properties استخراج نمی‌شود
- راه‌حل: بررسی کنید که فیلد نام در properties وجود دارد

**اگر `Logo API response` نشان می‌دهد `success: false`:**
- بررسی کنید که `available_logos` چه نام‌هایی دارد
- مقایسه کنید با `requested_name`
- اگر نام‌ها متفاوت هستند، مشکل از تطابق نام است

---

### مرحله 5: بررسی Network Tab

1. در Developer Tools به تب **Network** بروید
2. صفحه را refresh کنید
3. روی محله کلیک کنید
4. درخواست `/api/neighborhood-logo` را پیدا کنید
5. روی آن کلیک کنید و Response را ببینید

**اگر Status Code 200 است:**
- بررسی کنید که `success: true` است
- بررسی کنید که `logo_filename` وجود دارد

**اگر Status Code 404 است:**
- مشکل: Route پیدا نشد
- راه‌حل: بررسی کنید که route در app.py وجود دارد

**اگر Status Code 400 است:**
- مشکل: پارامترها ارسال نشده‌اند
- راه‌حل: بررسی کنید که `map_id` و `neighborhood_name` در URL هستند

---

### مرحله 6: تست مستقیم API

```bash
# در سرور
curl "http://localhost:5003/api/neighborhood-logo?map_id=YOUR_MAP_ID&neighborhood_name=YOUR_NEIGHBORHOOD_NAME"
```

**مثال:**
```bash
curl "http://localhost:5003/api/neighborhood-logo?map_id=abc123&neighborhood_name=تهران%203"
```

**اگر پاسخ `success: false` است:**
- بررسی کنید که `available_logos` چه نام‌هایی دارد
- مقایسه کنید با `requested_name`
- اگر نام‌ها متفاوت هستند، مشکل از تطابق نام است

---

### مرحله 7: بررسی فایل‌های لوگو

```bash
# در سرور
cd /var/www/regions-map-app/regions-map-app
ls -la uploads/regions/logos/

# بررسی فایل‌های JSON
cat uploads/regions/logos/*.json | grep -A 3 "YOUR_MAP_ID"
```

**اگر فایل JSON وجود ندارد:**
- مشکل: لوگو ذخیره نشده
- راه‌حل: دوباره آپلود کنید

**اگر فایل JSON وجود دارد اما فایل عکس نیست:**
- مشکل: فایل عکس حذف شده
- راه‌حل: دوباره آپلود کنید

---

### مرحله 8: تست نمایش فایل عکس

```bash
# در سرور
curl "http://localhost:5003/uploads/logos/LOGO_FILENAME.jpg"
```

**اگر 404 می‌دهد:**
- مشکل: فایل عکس وجود ندارد یا route مشکل دارد
- راه‌حل: بررسی کنید که فایل در `uploads/regions/logos/` وجود دارد

**اگر فایل نمایش داده می‌شود:**
- مشکل از JavaScript یا تطابق نام است

---

## 🐛 مشکلات رایج و راه‌حل‌ها

### مشکل 1: `selectedMapId` خالی است

**علت:** `map_id` در URL نیست

**راه‌حل:**
- مطمئن شوید که از `/map/{map_id}` یا `/?map_id={map_id}` استفاده می‌کنید
- بررسی کنید که `selected_map_id` در template set می‌شود

---

### مشکل 2: نام محله تطابق ندارد

**علت:** نامی که در آپلود استفاده شده با نامی که در sidebar استخراج می‌شود متفاوت است

**راه‌حل:**
- در Console، `requested_name` و `available_logos` را مقایسه کنید
- اگر نام‌ها متفاوت هستند، از نام دقیق استفاده کنید
- یا نام محله را در پنل ادمین ویرایش کنید

---

### مشکل 3: فایل عکس 404 می‌دهد

**علت:** فایل عکس وجود ندارد یا route مشکل دارد

**راه‌حل:**
```bash
# بررسی وجود فایل
ls -la uploads/regions/logos/LOGO_FILENAME.jpg

# اگر وجود ندارد، دوباره آپلود کنید
# اگر وجود دارد، بررسی route:
grep -n "uploads/logos" app.py
```

---

### مشکل 4: لوگو برای همه محلات یکسان است

**علت:** منطق تطابق نام مشکل دارد

**راه‌حل:**
- بررسی کنید که `get_neighborhood_key` برای هر محله کلید منحصر به فرد می‌سازد
- بررسی کنید که نام محله در JSON درست ذخیره می‌شود

---

## 📝 چک‌لیست دیباگ

- [ ] کد آپدیت شده است
- [ ] سرویس‌ها restart شده‌اند
- [ ] لوگو جدید آپلود شده است
- [ ] Console لاگ‌ها را بررسی کرده‌ام
- [ ] Network tab را بررسی کرده‌ام
- [ ] API را مستقیماً تست کرده‌ام
- [ ] فایل‌های لوگو را بررسی کرده‌ام
- [ ] فایل عکس را مستقیماً تست کرده‌ام

---

## 💡 نکات مهم

1. ✅ **همیشه Console را باز کنید** - لاگ‌ها اطلاعات مهمی می‌دهند
2. ✅ **Network tab را بررسی کنید** - می‌توانید ببینید که درخواست‌ها چه پاسخی می‌دهند
3. ✅ **نام محله را دقیق یادداشت کنید** - ممکن است فاصله یا کاراکترهای خاص داشته باشد
4. ✅ **مقایسه کنید** - `requested_name` را با `available_logos` مقایسه کنید

---

**اگر هنوز مشکل دارید، لاگ‌های Console و Network را ارسال کنید!**

