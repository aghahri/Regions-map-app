# راهنمای حل مشکل Permission Denied

## مشکل:
`Permission denied` - فایل دسترسی اجرا ندارد

## راه‌حل:

### 1. دادن دسترسی اجرا:

```bash
chmod +x restore_links_from_tar.sh
```

### 2. اجرای مجدد:

```bash
./restore_links_from_tar.sh
```

---

## یا اجرای مستقیم با bash:

```bash
bash restore_links_from_tar.sh
```

---

**موفق باشید! 🚀**

